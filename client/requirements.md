## Packages
recharts | For the score radial gauge and summary charts
framer-motion | For smooth page transitions and card animations
clsx | For conditional class names (standard utility)
tailwind-merge | For merging tailwind classes (standard utility)

## Notes
The backend provides `audits` with `results` (full JSON) and `summary` (counts).
Visual preview might be tricky due to X-Frame-Options, so we will focus on a high-quality list view of issues first, with an iframe as a progressive enhancement if allowed.
